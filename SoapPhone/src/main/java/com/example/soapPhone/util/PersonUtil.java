package com.example.soapPhone.util;

import com.example.soapPhone.model.Person;
import org.springframework.boot.CommandLineRunner;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

import java.time.LocalDate;
import java.time.Month;
import java.util.List;

/*@Configuration
public class PersonUtil {
    @Bean
    CommandLineRunner commandLineRunner(Person p){
        return args -> {
            new Person("bob", "+7", LocalDate.of(2001, Month.JANUARY, 5), 232);
        };
    } ;

    //new Person("Bob", List.of("+799"), LocalDate.of(2001, Month.JANUARY,4), (long) 21);
}*/
